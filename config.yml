---
connections:
  enabled: true

cpu:
  enabled: true

disk:
  enabled: true
#  devices: ["/dev/sda1"] #If you want to enable for certain disk devices

load:
  enabled: true

memory:
  enabled: true

network:
  enabled: true

console:
  enabled: true

jetty:
  enabled: true
  port: 8092

riemann:
  enabled: false
  host: 127.0.0.2
  port: 5555

http:
  enabled: true
  url: http://localhost:3000/collect
  headers: {X-Access-Token: t0oS3cr3t}
